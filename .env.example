# Medical Literature Aggregator Configuration

# Application URL (required for API calls)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cron Job Secret (for securing cron endpoints)
# Generate with: openssl rand -base64 32
CRON_SECRET=your-secret-token-here

# Email Configuration (optional - for notifications)
# Using Resend (https://resend.com)
RESEND_API_KEY=re_xxxxxxxxxxxxx
NOTIFICATION_EMAIL=your-email@example.com

# Or using SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourdomain.com
SMTP_TO=recipient@example.com

# Database Configuration (optional - for storing articles)
# Option 1: Vercel KV (Redis)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Option 2: Vercel Postgres
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Option 3: MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# Option 4: Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Impact Factor Settings
MIN_IMPACT_FACTOR=10.0

# Fetch Settings
DEFAULT_FETCH_DAYS=1
MAX_ARTICLES_PER_CATEGORY=50

# Rate Limiting
PUBMED_RATE_LIMIT_MS=500
OPENALEX_RATE_LIMIT_MS=100
